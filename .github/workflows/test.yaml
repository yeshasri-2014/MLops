name: MLops
on: [push]
jobs:
  run:
    runs-on: [self-hosted,Linux,X64]
    steps:
      - uses: actions/checkout@v2
      - name: Create CML report
        run: |
          /bin/echo 'Hi from CML!' >> report.md
          cml-send-comment report.md
